{
    "name": "ropeybot",
    "license": "Apache-2.0",
    "private": "true",
    "scripts": {
        "preinstall": "cd src && pnpm install",
        "types": "tsc",
        "prettier": "prettier -c .",
        "compile:bc-bot": "cd src && pnpm run compile",
        "bundle": "pnpm run compile:bc-bot && pnpm run build && pnpm run build:min",
        "build": "esbuild bin/main.ts --bundle --platform=node --outfile=dist/bundle.js --sourcemap",
        "build:min": "esbuild bin/main.ts --bundle --platform=node --outfile=dist/bundle.min.js --sourcemap --minify",
        "start": "pnpm run compile:bc-bot && tsx bin/main.ts",
        "docker": "pnpm run bundle && docker build -t ropeybot --no-cache ."
    },
    "dependencies": {
        "bc-bot": "link:src",
        "bc-stubs": "130.0.0",
        "lodash": "^4.17.21",
        "lz-string": "^1.5.0",
        "prettier": "^3.7.4"
    },
    "devDependencies": {
        "@tsconfig/node18": "^18.2.6",
        "@types/lodash": "^4.17.21",
        "@types/node": "^20.19.27",
        "esbuild": "^0.27.2",
        "tsx": "^4.21.0",
        "typescript": "^5.9.3"
    }
}
